#!/usr/bin/env bash

set -euo pipefail

case "${1:-}" in
    '-d' | '-D' | '--decode')
        echo "${@:2}" | base64 -d
        exit 0
    ;;
    '-e' | '--encode')
        echo "${@:2}" | base64
        exit 0
    ;;
esac

echo "$@" | base64
exit 0
